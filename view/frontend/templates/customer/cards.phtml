<?php
/** @var \ParadoxLabs\TokenBase\Block\Customer\Cards $block */
/** @var \Magento\Framework\Escaper $escaper */
?>
<div class="block block-cards-list">
    <div class="block-title">
        <h2><?= $escaper->escapeHtml($block->getPaymentMethodTitle()); ?></h2>
    </div>
    <div class="block-content items checkout-payment-method" data-mage-init='{"tokenbaseConfirmation": {}}'>
        <?php $cards = $block->getCards(); ?>
        <?php if (!empty($cards)): ?>
            <?php /** @var \ParadoxLabs\TokenBase\Model\Card $card */ ?>
            <?php foreach ($cards as $card): ?>
                <?php $card = $card->getTypeInstance(); ?>
                <fieldset class="box-address-billing payment-method-title fieldset<?php if ($card->getExpires() != '' && strtotime((string)$card->getExpires()) < time()): ?> expired<?php endif; ?>">
                    <legend class="box-title legend">
                        <?php if (!empty($card->getType())): ?>
                            <img class="credit-card-type payment-icon" src="<?= $escaper->escapeUrl($block->getViewFileUrl('Magento_Payment::images/cc/' . strtolower((string)$card->getType()) . '.png')); ?>" alt="<?= $escaper->escapeHtmlAttr($block->getCcTypeLabel($card)); ?>" />
                        <?php endif; ?>
                        <strong class="payment-icon">
                            <span>
                                <?= $escaper->escapeHtml($card->getLabel(empty($card->getType()))); ?>
                            </span>
                        </strong>
                        <?php if (strtotime((string)$card->getExpires()) > time()): ?>
                            <small><?= $escaper->escapeHtml(__('(Expires %1)', date('m/Y', (int)strtotime((string)$card->getExpires())))); ?></small>
                        <?php endif; ?>
                    </legend>
                    <div class="box-content">
                        <address>
                            <?= /* @noEscape */ $block->getFormattedCardAddress($card->getAddressObject()); ?>
                        </address>
                        <div class="item actions box-actions">
                            <?php if ($card->isInUse()): ?>
                                <abbr title="<?= $escaper->escapeHtmlAttr(__('This payment record cannot be modified while associated with pending orders.')); ?>"><?= $escaper->escapeHtml(__('Card In Use')); ?></abbr>
                            <?php else: ?>
                                <a class="action edit" href="<?= $escaper->escapeUrl($block->getUrl('*/*', ['_secure' => true, 'form_key' => $block->getFormKey(), 'method' => $card->getMethod(), 'id' => $card->getHash()])) ?>"><span><?= $escaper->escapeHtml(__('Edit')) ?></span></a>
                                <a class="action delete" href="<?= $escaper->escapeUrl($block->getUrl('*/*/delete', ['_secure' => true, 'form_key' => $block->getFormKey(), 'method' => $card->getMethod(), 'id' => $card->getHash()])) ?>"><span><?= $escaper->escapeHtml(__('Delete')) ?></span></a>
                            <?php endif; ?>
                        </div>
                    </div>
                </fieldset>
            <?php endforeach; ?>
        <?php else: ?>
            <p><?= $escaper->escapeHtml(__('You do not have any payment data saved yet.')) ?></p>
        <?php endif; ?>
    </div>
</div>
